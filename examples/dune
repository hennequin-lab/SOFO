(executables
 (names linear_regression lorenz mlp_classification_prune mlp_classification_sparse_net)
 (libraries cmdargs domainslib bos sofo owl owl-ode)
 (preprocess
  (pps ppx_sexp_message ppx-forward-torch)))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
